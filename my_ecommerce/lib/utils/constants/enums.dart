//List of enums 
// theycannot be created inside a class

enum Textsizes {
  small,
  medium,
  large,
}

enum OrderStatus {
  pending,
  shipped,
  delivered,
  cancelled,
}
enum PaymentMethod {
  creditCard,
  debitCard,
  paypal,
  cashOnDelivery,
  pytm,
  visa
}